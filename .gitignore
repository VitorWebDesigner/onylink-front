# dependências
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# builds nativos
ios/
android/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# ambiente (segredos — NUNCA commitar)
.env
.env.*.local

# chave de service account do Firebase (FCM V1) — SEGREDO, vive fora do repo
*firebase-adminsdk*.json
onylink-7bbdf-*.json
hotmart-*.json

# logs / SO
npm-debug.*
yarn-debug.*
yarn-error.*
*.log
.DS_Store

# pacotes zipados locais
*.zip
